import 'package:flutter/material.dart';

class CoursesWithListTile extends StatelessWidget {
  const CoursesWithListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}